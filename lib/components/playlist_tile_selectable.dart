import 'package:flutter/widgets.dart';

class PlaylistTileSelectable extends StatelessWidget {
  const PlaylistTileSelectable({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
